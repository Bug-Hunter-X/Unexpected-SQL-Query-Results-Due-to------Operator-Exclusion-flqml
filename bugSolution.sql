```sql
SELECT * FROM employees WHERE department = 'Sales' AND salary >= 100000;
```
This revised query uses the `>=` operator to correctly include employees with salaries greater than or equal to 100000, ensuring a complete and accurate result set.